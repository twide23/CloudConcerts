@model IEnumerable<CloudConcerts3.Models.GigGenre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Genre.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gig.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Genre.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gig.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GigGenreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GigGenreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GigGenreID })
        </td>
    </tr>
}

</table>
