@model IEnumerable<CloudConcerts3.Models.ListenerGenre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Genre.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Listener.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Genre.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Listener.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ListenerGenreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ListenerGenreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ListenerGenreID })
        </td>
    </tr>
}

</table>
