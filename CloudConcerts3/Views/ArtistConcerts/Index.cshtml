@model IEnumerable<CloudConcerts3.Models.ArtistConcert>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artist.StageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Concert.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artist.StageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Concert.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtistConcertID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtistConcertID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtistConcertID })
        </td>
    </tr>
}

</table>
